name: Deploy Documentation

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 📌 Instalar dependencias del frontend
      - name: Install Frontend Dependencies
        run: HUSKY=0 npm install
        working-directory: gestion-negocio-frontend

      # 📌 Instalar dependencias de Docusaurus
      - name: Install Docusaurus Dependencies
        run: npm install
        working-directory: website  # ✅ Instala dependencias antes de compilar

      # 📌 Generar documentación del frontend
      - name: Generate Frontend Docs
        run: npm run docs
        working-directory: gestion-negocio-frontend

      # 📌 Construir Docusaurus
      - name: Build Docusaurus
        run: npm run build
        working-directory: website  # ✅ Ahora debería encontrar "docusaurus"

      # 📌 Desplegar en GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: website/build
