name: Deploy Documentation

on:
  push:
    branches:
      - main  # Asegúrate de que el workflow se ejecute solo en la rama principal

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 📌 Instalar dependencias del frontend en la carpeta correcta
      - name: Install Frontend Dependencies
        run: npm install
        working-directory: gestion-negocio-frontend  # ✅ Ubicación correcta

      # 📌 Generar documentación del frontend
      - name: Generate Frontend Docs
        run: npm run docs
        working-directory: gestion-negocio-frontend  # ✅ Ejecutar en la carpeta correcta

      # 📌 Construir Docusaurus (si la documentación está en la carpeta "website")
      - name: Build Docusaurus
        run: npm run build
        working-directory: website  # ✅ Ajusta si Docusaurus está en otra carpeta

      # 📌 Desplegar en GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: website/build  # ✅ Asegurar que la documentación esté en la carpeta correcta
